apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lang-api.fullname" . }}-test-connection"
  labels:
    {{- include "lang-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lang-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
